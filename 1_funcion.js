//Declaracion de Funcion (se puede llamar antes de declararla)

function example(a, b, c) {
  return a + b + c;
}

// Expresion de Función (no se puede llamar antes de declararla)

const example = function (a, b, c) {
  return a + b + c;
};
